# v0.1.0-alpha.4 release notes

## Resolved

- The release package now includes ExLaunch's generated `main.npdm`, allowing installer-only Atmosphere setups to boot with the required kernel capabilities.
